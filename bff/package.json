{
  "name": "signal-bff",
  "version": "0.1.0",
  "private": true,
  "description": "Backend-for-frontend that serves component-tree screens and AI-backed actions to the Signal mobile client.",
  "type": "commonjs",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json && mkdir -p dist/llm/prompts && cp src/llm/prompts/*.txt dist/llm/prompts/",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.68.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.10",
    "tsx": "^4.16.2",
    "typescript": "^5.5.4"
  }
}
